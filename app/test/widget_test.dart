import 'package:flutter_test/flutter_test.dart';

void main() {
  // Basic smoke test — skipped since LarqBleService requires platform BLE.
  testWidgets('App builds with scan screen', (WidgetTester tester) async {
    // TODO: Test with a mock LarqBleService
  }, skip: true);
}
